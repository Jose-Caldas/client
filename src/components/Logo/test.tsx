import { render, screen } from "@testing-library/react";

//renderizar o component 'render'
//selecionar o elemento 'screen' (queries) - getByLabel, getByText ...
//expect - assertion - assercão, análise, comparação (neste caso renderizar a logo branca)

import Logo from ".";

describe("<Logo/>", () => {
  it("should render a whote label by default", () => {});
});
